        <!-- Tools/SDKs -->
<% if (language === 'groovy') { -%>
        <groovy.version><%= versions.languages.groovy %></groovy.version>
<% } -%>
<% if (language === 'kotlin') { -%>
        <kotlin.version><%= versions.languages.kotlin %></kotlin.version>
<% } -%>
<% if (language === 'scala') { -%>
        <scala.version><%= versions.languages.scala %></scala.version>
<% } -%>
        <node.version><%= versions.frontEnd.node %></node.version>
        <yarn.version><%= versions.frontEnd.yarn %></yarn.version>
