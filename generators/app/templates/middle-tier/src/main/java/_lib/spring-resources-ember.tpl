        // The client application build uses an "assets" directory to contain CSS, JS and media files
        registry.addResourceHandler("/assets/**").addResourceLocations("classpath:/app/assets/");
